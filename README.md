# (HL)全球发布 — Render 私有测试版

部署请阅读 [RENDER_DEPLOY.md](RENDER_DEPLOY.md)。

本版本支持视频上传、持久化数据库、任务创建与定时阻塞；不支持真实社交平台发帖，不是完整商业 SaaS。

本地开发：复制 `.env.example` 到 `.env`，设置两个不同的高强度密码，然后 `docker compose up --build -d`。
